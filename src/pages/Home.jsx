import { Section } from 'components/App.styled';
import { Hero } from 'components/Hero/Hero';

const Home = () => {
    return (
        <Section>
            <Hero />
        </Section>
    );
};

export default Home;
